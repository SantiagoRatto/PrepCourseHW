function ordenarArray(array) {
  // Ordena los elementos del areglo array de menor a mayor.
  // Devuelve el arreglo resultante.
  // Tu código:
  var ordenados = array.toSorted();
  return (ordenados);
}

module.exports = ordenarArray;
